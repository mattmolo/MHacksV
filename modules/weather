
<!DOCTYPE html>
<html lang="en">
<head>

</head>
<body>
<style>
  html, body {
    width: 100%;
    height: 100%;
    padding: 0;
    margin: 0;
  }
</style>
<script type="application/javascript">


function getQueryParams(qs) {
    qs = qs.split("+").join(" ");

    var params = {}, tokens,
        re = /[?&]?([^=]+)=([^&]*)/g;

    while (tokens = re.exec(qs)) {
        params[decodeURIComponent(tokens[1])]
            = decodeURIComponent(tokens[2]);
    }

    return params;
}

var query = getQueryParams(document.location.search);
$.ajax({
  dataType: "jsonp",
  url: "http://api.openweathermap.org/data/2.5/weather?q=" + query.location + "&callback=test&units=" + query.unit,
  data: data,
  callback: "test",
  success: function(data){
    console.log(data)
  }
});

</script>

</body>


</html>





